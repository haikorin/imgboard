# Leti.ch - Имиджборд

Веб-приложение для создания и обсуждения постов с поддержкой загрузки файлов, комментариев и системы голосования.

## 🚀 Возможности

- **Посты с файлами**: Создание постов с текстом и/или файлами (изображения, видео, аудио, документы)
- **Аудио альбомы**: Загрузка нескольких аудио файлов в один пост с переключением между треками
- **Комментарии**: Комментирование постов с возможностью удаления своих комментариев
- **Система голосования**: Апвоуты и даунвоуты для постов
- **Авторизация**: Регистрация и вход пользователей с JWT токенами
- **Темы оформления**: Светлая и тёмная тема
- **Сортировка и фильтрация**: Сортировка по дате и количеству апвоутов, фильтрация по категориям
- **Страница поста**: Отдельная страница для просмотра поста с комментариями
- **Управление постами**: Удаление своих постов

## 🛠 Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация пользователей
- **Mutagen** - извлечение метаданных из аудио файлов
- **Uvicorn** - ASGI сервер

### Frontend
- **Vanilla JavaScript** - без фреймворков
- **HTML5/CSS3** - разметка и стили
- **Fetch API** - взаимодействие с бэкендом

### Инфраструктура
- **Docker** - контейнеризация приложения
- **Docker Compose** - оркестрация сервисов

## 📁 Структура проекта

```
.
├── backend/                 # Backend приложение
│   ├── api/                # API эндпоинты
│   │   ├── auth.py        # Аутентификация
│   │   ├── posts.py       # Посты
│   │   ├── comments.py    # Комментарии
│   │   ├── users.py       # Пользователи
│   │   └── metadata.py    # Метаданные аудио
│   ├── models.py          # SQLAlchemy модели
│   ├── schemas.py         # Pydantic схемы
│   ├── database.py        # Настройка БД
│   ├── dependencies.py    # Зависимости FastAPI
│   ├── auth_utils.py      # Утилиты для аутентификации
│   ├── file_utils.py      # Утилиты для работы с файлами
│   ├── migrations.py      # Миграции БД
│   ├── main.py            # Точка входа приложения
│   ├── requirements.txt   # Python зависимости
│   └── Dockerfile         # Docker образ бэкенда
├── frontend/              # Frontend приложение
│   ├── index.html        # Главная страница
│   ├── js/               # JavaScript файлы
│   │   ├── env.js        # Конфигурация
│   │   ├── auth.js       # Авторизация
│   │   ├── posts.js      # Работа с постами
│   │   ├── post-view.js  # Страница поста
│   │   ├── upload.js     # Загрузка постов
│   │   ├── theme.js      # Переключение темы
│   │   └── init.js       # Инициализация
│   └── style/
│       └── index.css     # Стили
├── docker-compose.yml     # Docker Compose конфигурация
└── README.md             # Документация
```

## 🔧 Установка и запуск

### Требования

- Docker и Docker Compose
- Или Python 3.11+ и PostgreSQL 15+

### Запуск с Docker (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd web
```

2. Создайте файл `.env` (опционально, для кастомизации):
```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=imageboard
POSTGRES_PORT=5432
BACKEND_PORT=8000
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

3. Запустите приложение:
```bash
docker-compose up -d
```

4. Приложение будет доступно по адресам:
   - Frontend: http://localhost:8000
   - Backend API: http://localhost:8000/api
   - Swagger UI: http://localhost:8000/docs
   - ReDoc: http://localhost:8000/redoc

### Запуск без Docker

1. Установите зависимости:
```bash
cd backend
pip install -r requirements.txt
```

2. Настройте базу данных PostgreSQL и создайте файл `.env`:
```env
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/imageboard
SECRET_KEY=your-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

3. Запустите миграции (автоматически при старте):
```bash
python migrations.py
```

4. Запустите сервер:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

5. Откройте frontend в браузере (настройте веб-сервер или используйте простой HTTP сервер):
```bash
cd frontend
python -m http.server 8080
```

## 📚 API Документация

После запуска приложения API документация доступна по адресам:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты

#### Аутентификация
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход и получение JWT токена

#### Посты
- `GET /posts` - Получить список постов
- `GET /posts/{post_id}` - Получить конкретный пост
- `POST /posts` - Создать новый пост (требует авторизации)
- `PUT /posts/{post_id}` - Обновить пост (требует авторизации)
- `DELETE /posts/{post_id}` - Удалить пост (только свой, требует авторизации)
- `POST /posts/{post_id}/upvote` - Апвоут поста
- `POST /posts/{post_id}/downvote` - Даунвоут поста
- `GET /posts/{post_id}/file` - Получить файл поста
- `GET /posts/{post_id}/files/{file_id}` - Получить конкретный файл поста

#### Комментарии
- `GET /posts/{post_id}/comments` - Получить комментарии к посту
- `POST /posts/{post_id}/comments` - Создать комментарий (требует авторизации)
- `DELETE /posts/{post_id}/comments/{comment_id}` - Удалить комментарий (только свой, требует авторизации)

#### Пользователи
- `GET /users/me/profile` - Получить профиль текущего пользователя (требует авторизации)

#### Метаданные
- `GET /posts/{post_id}/metadata` - Получить метаданные аудио файла
- `GET /posts/{post_id}/files/{file_id}/metadata` - Получить метаданные конкретного файла

## 🎨 Функциональность

### Посты

- **Создание постов**: Текст (опционально) + один или несколько файлов
- **Типы файлов**:
  - Изображения (JPEG, PNG, GIF, WebP, BMP, TIFF) - отображаются в браузере
  - Видео (MP4, WebM, OGG, MOV, AVI, MKV) - встроенный видеоплеер
  - Аудио (MP3, OGG, WAV, WebM, FLAC, AAC, M4A) - встроенный аудиоплеер с метаданными
  - Другие файлы - доступны для скачивания
- **Аудио альбомы**: Несколько аудио файлов в одном посте с переключением треков
- **Метаданные**: Автоматическое извлечение обложки, названия, исполнителя из аудио файлов
- **Максимальный размер файла**: 200 МБ

### Комментарии

- Комментирование постов
- Отображение автора и даты комментария
- Удаление своих комментариев
- Мягкое удаление (soft delete)

### Система голосования

- Апвоуты и даунвоуты для постов
- Сортировка по количеству апвоутов
- Отображение текущего рейтинга

### Интерфейс

- **Темы**: Светлая и тёмная тема с переключением
- **Сортировка**: По дате (возрастание/убывание) и по апвоутам (возрастание/убывание)
- **Фильтрация**: По категориям (всё, картинки, видео, аудио, текст, другое)
- **Адаптивная вёрстка**: Grid layout для постов
- **Сворачивание**: Длинные тексты и списки треков сворачиваются с кнопкой "показать все"

## 🔐 Безопасность

- JWT токены для аутентификации
- Хеширование паролей с помощью bcrypt
- Проверка прав доступа (пользователи могут удалять только свои посты и комментарии)
- Валидация данных через Pydantic
- Защита от XSS через экранирование HTML

## 📝 Конфигурация

### Переменные окружения

- `DATABASE_URL` - URL подключения к базе данных
- `SECRET_KEY` - Секретный ключ для JWT (обязательно изменить в production!)
- `ALGORITHM` - Алгоритм шифрования JWT (по умолчанию HS256)
- `ACCESS_TOKEN_EXPIRE_MINUTES` - Время жизни токена в минутах (по умолчанию 30)

### Frontend конфигурация

Настройки находятся в `frontend/js/env.js`:
- `API_BASE` - URL бэкенда
- `POSTS_PER_PAGE` - Количество постов на странице
- `MAX_TEXT_LENGTH` - Максимальная длина текста до обрезки
- `MAX_VISIBLE_TRACKS` - Максимальное количество видимых треков в альбоме

## 🗄 База данных

### Таблицы

- **users** - Пользователи
- **posts** - Посты
- **post_files** - Файлы постов
- **comments** - Комментарии

Миграции выполняются автоматически при старте приложения через `migrations.py`.

## 🐛 Известные проблемы

- Нет поддержки вложенных комментариев (только плоская структура)
- Нет пагинации для комментариев (загружаются все сразу)
- Нет редактирования постов и комментариев

## 🚧 Планы на будущее

- [ ] Вложенные комментарии (threading)
- [ ] Редактирование постов и комментариев
- [ ] Пагинация комментариев
- [ ] Поиск по постам
- [ ] Теги для постов
- [ ] Админ-панель
- [ ] Модерация контента
- [ ] Уведомления о новых комментариях

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Leti.ch - Лучший имиджборд в мире!

---

**Примечание**: В production обязательно измените `SECRET_KEY` и настройте CORS для конкретных доменов!

